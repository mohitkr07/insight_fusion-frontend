import ChatBot from '@/components/ChatBot';

export default function ChatPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[var(--background)] transition-colors">
      <ChatBot />
    </main>
  );
}
